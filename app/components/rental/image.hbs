<button {{on "click" this.toggleSize}} type="button" class="image {{if this.isLarge "large"}}">
    <img ...attributes>
    {{#if this.isLarge}}
        <small>View Smaller</small>
    {{else}}
        <small>View Larger</small>
    {{/if}}
</button>